# autosuggestt

Verifying my git set-up :)


changes flow for pushing 
1. add( plus button)
2. commit with message
3. push/sync

**commands for pushing into github in terminal :
git add -A
git commit -m "some message"
git push